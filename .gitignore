.env
.env.*
!.env.example
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
node_modules/
dist/
*.tsbuildinfo
*.log

# A4: database dumps. Never in git — they hold operational data and are unencrypted.
backups/
*.sql
*.sql.gz
*.sql.gz.*

# Build output packaged as a file rather than a directory — `dist/` above does not match it.
extension/dist.zip

# Local dev server pids and logs (scripts/dev.sh)
.dev/
