[pytest]
# Every test here is a plain function. Without this, pytest tries to collect the product's
# own TestSendService (A4) as a test class and warns about its constructor.
python_classes =
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
